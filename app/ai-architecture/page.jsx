import Header from '../../components/Header';import Footer from '../../components/Footer';import strategy from '../../data/providerStrategy.json';export default function Page(){return <main className='container'><Header/><section className='hero'><div><span className='badge'>Provider Decision</span><h1>Kosten- und Providerstrategie entschieden.</h1><p>Kein eigener GPU-Stack in Phase 5. Fokus: schnelle, kontrollierbare, API-basierte Infrastruktur.</p></div><div className='card'>{Object.entries(strategy).map(([k,v])=><p key={k}><strong>{k}:</strong> {String(v)}</p>)}</div></section><Footer/></main>}
