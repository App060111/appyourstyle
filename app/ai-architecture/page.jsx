import Header from '../../components/Header';import Footer from '../../components/Footer';import providers from '../../data/aiProviders.json';export default function Page(){return <main className='container'><Header/><section className='hero'><div><span className='badge'>AI Provider Layer</span><h1>KI-übergreifende Architektur.</h1><p>Text, Vision, Image, Search, Vector und Workflow bleiben modular austauschbar.</p></div><div className='card'><h2>Provider Selection</h2>{Object.entries(providers).map(([k,v])=><p key={k}><strong>{k}:</strong> {v.join(', ')}</p>)}</div></section><Footer/></main>}
