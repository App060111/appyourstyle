import{extractProductFromUrl,analyzeOutfit}from'../../../../lib/data';import{openAIAnswer}from'../../../../lib/ai/openai';import{replicateOutfitImage}from'../../../../lib/ai/replicate';export async function POST(req){const body=await req.json();const products=(body.links||[]).map((l,i)=>extractProductFromUrl(l,i+1)).filter(x=>x.url||x.error);const analysis=analyzeOutfit(products,body.userSize||'M');const aiExplanation=await openAIAnswer({question:'Erkläre dieses Outfit kurz mit Größenrisiko.',context:{products,analysis}});const prompt=`Neutral AI fashion outfit preview. Items: ${products.map(p=>p.category).join(', ')}. Do not use logos. Illustrative only.`;const image=await replicateOutfitImage({prompt});return Response.json({products,analysis,aiExplanation,image})}
