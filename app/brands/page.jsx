import Header from '../../components/Header';import Footer from '../../components/Footer';import BrandLogo from '../../components/BrandLogo';import {brands,fitProfiles} from '../../lib/data';export const metadata={title:'Alle Marken | AppYourStyle'};export default function Brands(){const groups=[...new Set(brands.map(b=>b.categoryGroup))];return <main className="container"><Header/><section className="hero"><div><span className="badge">Brand Intelligence</span><h1>Alle Marken.</h1><p>Markenprofile mit Segment, Fit-Typ, Datenstatus, Region und Aktualisierung.</p></div><div className="card"><h2>{brands.length} Marken</h2><p>Luxury, Sportswear, Fast Fashion, Denim, Outdoor, Streetwear, Schuhe und Womenswear.</p></div></section>{groups.map(g=><section className="seoSection" key={g}><h2>{g}</h2><div className="brandGrid">{brands.filter(b=>b.categoryGroup===g).map(b=>{const fp=fitProfiles[b.id];return <div className="brandCard" key={b.id}><BrandLogo brand={b}/><h3>{b.name}</h3><p>{b.segment}</p><p>{fp?.tendency}</p><span className="brandPill">{fp?.sourceType}</span></div>})}</div></section>)}<Footer/></main>}
