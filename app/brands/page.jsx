import Header from'../../components/Header';import Footer from'../../components/Footer';import BrandLogo from'../../components/BrandLogo';import{brands,fitProfiles}from'../../lib/data';export const metadata={title:'Alle Marken | AppYourStyle'};export default function Brands(){const groups=[...new Set(brands.map(b=>b.categoryGroup))];return <main className='container'><Header/><section className='hero'><div><span className='badge'>Brand Knowledge Graph</span><h1>Alle Marken & Marktplätze.</h1><p>Fit-Profile, Regionen, SourceType und Confidence für globale Fashion.</p></div><div className='card'><h2>{brands.length} Einträge</h2><p>Luxury, Sneaker, Marketplace, Streetwear, Sportswear und internationale Systeme.</p></div></section>{groups.map(g=><section className='seoSection' key={g}><h2>{g}</h2><div className='brandGrid'>{brands.filter(b=>b.categoryGroup===g).map(b=>{const f=fitProfiles[b.id];return <div className='brandCard' key={b.id}><BrandLogo brand={b}/><h3>{b.name}</h3><p>{b.region}</p><p>{f?.tendency}</p><span className='pill'>{f?.sourceType}</span></div>})}</div></section>)}<Footer/></main>}
