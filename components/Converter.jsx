'use client';import {useMemo,useState} from 'react';import {brands} from '../lib/data';export default function Converter(){const [fromBrand,setFromBrand]=useState('nike'),[toBrand,setToBrand]=useState('zara'),[inputSize,setInputSize]=useState('M'),[category,setCategory]=useState('tops'),[query,setQuery]=useState(''),[result,setResult]=useState(null);const filtered=useMemo(()=>brands.filter(b=>b.name.toLowerCase().includes(query.toLowerCase())).slice(0,18),[query]);async function run(){const res=await fetch('/api/convert',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({fromBrand,toBrand,inputSize,category})});setResult(await res.json())}return <div className="card converterCard" id="converter"><span className="badge">Instant Size Intelligence</span><h2>Welche Größe passt?</h2><p>Schneller Größenvergleich mit Quelle, Confidence und Fit-Logik.</p><div className="converter"><div className="searchBox"><span className="searchIcon">⌕</span><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Marke suchen: Nike, Zara, Gucci..." /></div>{query && <select size="4" onChange={e=>{setFromBrand(e.target.value);setQuery('')}}>{filtered.map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</select>}<select value={fromBrand} onChange={e=>setFromBrand(e.target.value)}>{brands.map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</select><select value={inputSize} onChange={e=>setInputSize(e.target.value)}>{['XXS','XS','S','M','L','XL','XXL','3XL','4XL'].map(s=><option key={s}>{s}</option>)}</select><select value={toBrand} onChange={e=>setToBrand(e.target.value)}>{brands.map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</select><select value={category} onChange={e=>setCategory(e.target.value)}>{['tops','pants','shoes','kids','baby','dresses','denim'].map(c=><option key={c}>{c}</option>)}</select><button onClick={run}>Größe übersetzen</button></div>{result&&<div className="result"><div>{result.fromBrandName} {result.inputSize} entspricht ungefähr</div><strong>{result.toBrandName} {result.outputSize}</strong><p>{result.note}</p><div className="score"><span style={{width:result.confidencePercent}}></span></div><div className="resultMeta"><span>Quelle: {result.sourceType}</span><span>Confidence: {result.confidencePercent}</span><span>Risiko: {result.returnRisk}</span><span>Fit: {result.fitType}</span><span>Kategorie: {result.category}</span><span>Update: {result.updatedAt}</span></div></div>}</div>}
