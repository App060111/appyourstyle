'use client';import{useState}from'react';
export default function AISearch(){const[q,setQ]=useState('Welche Größe bei Temu wenn ich Nike M trage?');const[r,setR]=useState(null);async function ask(){const res=await fetch('/api/ai/answer',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question:q})});setR(await res.json())}return <div className='card'><span className='badge'>AI Search Engine</span><h2>Fashion Frage stellen</h2><textarea value={q} onChange={e=>setQ(e.target.value)}/><button onClick={ask}>Antwort erzeugen</button>{r&&<div className='result'><strong>{r.answer.mode}</strong><p>{r.answer.answer}</p><div className='meta'><span>Provider: {r.answer.provider}</span><span>Confidence: {Math.round((r.answer.confidence||0)*100)}%</span><span>Vector: {r.vector.mode}</span></div></div>}</div>}
