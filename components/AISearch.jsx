'use client';import{useState}from'react';
export default function AISearch(){const[q,setQ]=useState('Fällt Hoka größer aus als Nike?');const[r,setR]=useState(null);async function ask(){const res=await fetch('/api/search/ask',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question:q})});setR(await res.json())}return <div className="card"><span className="badge">AI Suche</span><h1 className="cardTitle">Frage stellen</h1><textarea value={q} onChange={e=>setQ(e.target.value)}/><button onClick={ask}>Antwort suchen</button>{r&&<div className="responseCard"><h2>{r.parsed.intent}</h2><p>{r.answer.answer}</p><div className="meta"><span>Kategorie: {r.parsed.category}</span><span>Marken: {r.parsed.brandsFound.join(', ')||'automatisch'}</span><span>Modus: {r.answer.mode}</span></div></div>}</div>}
