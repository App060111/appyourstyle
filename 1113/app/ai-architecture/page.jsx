import Header from '../../components/Header';import Footer from '../../components/Footer';import providers from '../../data/aiProviders.json';export const metadata={title:'AI Architecture | AppYourStyle'};export default function Page(){return <main className='container'><Header/><section className='hero'><div><span className='badge'>AI Provider Abstraction Layer</span><h1>KI-übergreifende Entwicklungsarchitektur.</h1><p>Die Plattform hängt nicht von einem Anbieter ab. Text, Vision, Image, Search, Vector und Workflow sind modular.</p></div><div className='card'><h2>Provider Selection</h2>{Object.entries(providers).map(([k,v])=><p key={k}><strong>{k}:</strong> {v.join(', ')}</p>)}</div></section><Footer/></main>}
